name: test
channels:
  - pytorch
  - conda-forge
  - bioconda
  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Package dependencies
  - pandas>=2.0
  - matplotlib>=3.7
  - numpy>=1.24
  - pyreadr>=0.4.7
  - pytorch
  - scikit-learn>=1.2
  - scipy>=1.10
  - seaborn>=0.12

    # Pip-only installs
  #- pip:
  #  - codecov
